# Review walkthrough — Vulcan (2026-06-21)

> Sessione di think-aloud su tutta l'app. Catturate le osservazioni e mappate sul backlog esistente ([piano-interventi.md](piano-interventi.md)).
> Tag stato:
> - 🆕 **nuovo** — non presente nel piano
> - 🔁 **già tracciato** — rimando all'ID esistente (in corso/da fare)
> - ⚠️ **ri-segnalato** — il piano lo dà ✅ ma l'utente lo vede ancora come problema → verificare regressione o efficacia del fix
>
> Le voci ⚠️ sono le più importanti: o c'è una regressione, o il fix non comunica.

---

## 🐞 Bug concreti

| # | Osservazione | Dove | Stato |
|---|---|---|---|
| R1 | **Tab Condimento vuota** — SISTEMICO: **17 stili su 28** (61%) hanno la tab del tutto vuota, non solo il ciaccino | `recipe-output.tsx:2061`, `topping-library.ts`, `pizza-engine.ts` | 🔬 **DIAGNOSI**: `default_topping_ref` di 17 stili usa nomenclatura `<concept>_<stile>` (es. `margherita_teglia_romana`) che non esiste in `TOPPING_LIBRARY` → `getToppingForStyle` ritorna `undefined` → `renderToppingSection` fa `return null` → tab vuota. 15 risolvibili rimappando al concept esistente; 2 (ciaccino, new_haven) richiedono contenuto nuovo. INT‑A1/A6 non coprivano questo. |

> **Stili rotti (R1)**: teglia_romana, pinsa_romana, bonci_teglia, focaccia_genovese, grandma_style, focaccia_recco, padellino_torino, pizza_baciata, ciaccino_senese, pizza_patate_porchetta, trancio_milanese, chicago_tavern, pizza_al_metro, new_haven_apizza, fugazzeta, california_style, greek_pan.
>
> ✅ **FATTO (3 parti):** (1) **difesa in profondità** in `recipe-output.tsx:2061` — se `default_topping_ref` non risolve, fallback sul topping più autentico invece di `return null`; (2) **rimappati 16 `default_topping_ref`** ai concept esistenti in `pizza-engine.ts` (margherita/bianca/diavola/ortolana/patate_porchetta/crescenza_recco/bianca_mortazza/fugazzeta); (3) **creato contenuto nuovo** per i 2 genuinamente orfani: concept+ricetta `ciaccino` (farcitura senese, R29) e `white_clam` (New Haven). Verificati 6 stili campione + deep-link. `tsc` pulito sui file toccati.
| R2 | **Card "salsicce e friarielli" non cliccabile** in Scopri (la margherita sì) | `topping-library.ts` | ✅ **FATTO** — non era il click: il link navigava ma il deep-link `?topping=salsiccia_friarielli` non si applicava (concept senza `ToppingRecipe`) → ricadeva su Margherita, identica alla card Margherita → "non succede nulla". Creato `salsiccia_friarielli_napoletana` (+ `cacio_e_pepe_crema`, `hawaiiana_classica` per le altre signature orfane). Verificato. |
| R3 | **Tasto "Inizia pizzata" colore sbagliato**: aspetto vetroso invece di verde | `liquid-dock.ts` | ✅ **FATTO** — bug CSS: `--cta-btn-bg` è già un gradiente (`--grad-sage`), annidato come color-stop dentro un altro `linear-gradient` → CSS invalido → background scartato → restava il vetro. Sostituito col colore solido `var(--cta)` nel gradiente. Verde verificato visivamente. |
| R4 | **Discrepanza Crea vs Esplora**: nel verticale Esplora mancano tasto pizzata e toggle nerd mode | `recipe.tsx`, `active-cook-widget.tsx` | 🔬 **DIAGNOSI — non è Crea vs Esplora**: il toggle nerd dipende da `vulcan_nerd_on` (flag **globale**, attivato solo dal Profilo → "Pizza Nerd"), identico nei due vertical. Il tasto pizzata si riduce per stili **non fattibili** con la cucina (es. canotto senza forno a legna). La differenza percepita era tra stile fattibile/non-fattibile e stato del flag, non tra i vertical. **Decisione di prodotto**: rendere il nerd più scopribile? mostrare perché il tasto pizzata è ridotto? |
| R5 | **Ricerca "boscaiola" non trova nulla** (né stili né topping) | `search-overlay.tsx` | ✅ **FATTO** — la ricerca indicizzava solo stili/farine/glossario; i chip "Ricette"/"Condimenti" erano pure mal mappati (→ guide/farine). Aggiunti i tipi `recipe` (signature) e `topping` (concept) all'indice, corretti i chip filtro e i deep-link. "boscaiola"/"salsiccia" ora trovano ricette + condimenti. Verificato. |
| R6 | **Toggle "modalità nerd" sparito** dalla modale parametri | `recipe-configurator.tsx` | 🔬 **DIAGNOSI — non è un bug**: il toggle nerd non è (mai stato) nella modale parametri; vive nelle sezioni Ricetta/Procedimento (`recipe-view.tsx`) e appare solo se il Pizza Nerd è attivo dal Profilo. **UX**: valutare se renderlo più scopribile / coerente. |

---

## ✨ Quick win UI (estetica / copy, basso rischio)

| # | Osservazione | Dove | Stato |
|---|---|---|---|
| R7 | **Home: troppo scroll** — "Make pizza not war" finisce troppo in fondo; compattare | `home.tsx` | 🔶 **PARZIALE** — ridotti gli spazi verticali dell'hero (`mb-6`→`mb-3`, `pb-6/8`→`pb-4/5`). Compattato senza stravolgere il respiro. Si può ridurre ancora il glow se serve. |
| R8 | **Branding**: far comparire il nome "Vulcan" oltre al logo | `home.tsx` | ✅ **FATTO** — wordmark "VULCAN" (uppercase, tracking, colore primary) sotto il logo nell'hero. |
| R9 | **Controlli in alto** (luogo + temperatura): contrasto e dimensioni troppo piccoli | `user-needs.tsx` (`SettingsSummaryBar`) | ⚠️ INT‑C1 ✅ — ma ancora percepiti piccoli/poco contrastati |
| R10 | **Pill forno**: label → "La tua cucina" (oggi "forno domestico"); allineamenti | `user-needs.tsx` | ✅ **FATTO** — chip cucina ora usa `cms.ui.kitchenTitle` ("La tua cucina", già localizzato 7 lingue); tipo forno + temp restano nell'icona e nel drawer. Verificato. |
| R11 | **Stepper temperatura cucina non centrato**; drawer cucina da rendere più compatto | `recipe-setup-panel.tsx`, `Stepper.tsx` | 🆕 |
| R12 | **Colore selezione incoerente**: lievito verde vs farine rosso, senza spiegazione | `user-needs.tsx` | ✅ **FATTO** — il chip lievito usava `--needs-yeast-bg-active` (verde/cta); ora usa `--chip-bg-active` come le farine. Selezione uniforme (terracotta). Verificato. |
| R13 | **Copy ridondante stili**: "i migliori per il tuo profilo" + "perfetti per te / ideali" | `cms-context.tsx` (`pages.styles.subtitle` + `perfect`) | 🔶 **DA ALLINEARE** — ridondanza confermata (header "I migliori per il tuo profilo" + categoria "Perfetti per te"). Fix proposto: sottotitolo header → criterio ("In base a forno, tempo e livello"). Richiede 7 locali → meglio decidere il wording prima di tradurre. |
| R14 | **Style detail sheet**: spaziatura/aria + button in alto a dx; 3 iconcine disallineate vs label | `style-detail-sheet.tsx` | ⚠️ INT‑C10 🔶 / C3 — rifinitura non chiusa |
| R15 | **Copy "Perché combacia col tuo setup"** troppo tecnico (è proprio il titolo introdotto da C3) | `style-detail-sheet.tsx` | ⚠️ INT‑C3 ✅ — il fix non convince a livello di tono |
| R16 | **Match "ci vuole un po' di corteggiamento"**: tono troppo cute | `recipe-match-card.tsx:33` | 🔶 **DA ALLINEARE (voce)** — è una scala intera con metafora "amore" coerente con l'icona cuoricino (Ottima intesa / scintilla / Cuore spezzato…). Hardcoded IT, non CMS. Non la cambio a pezzi: decisione di voce di brand. |
| R17 | **Modale parametri**: header (titolo, padding, centratura su breakpoint, posizione match) da raffinare; **3 card con label tagliate** su alcuni breakpoint | `recipe-configurator.tsx` | ⏸️ **DA GUARDARE INSIEME** — a viewport medio l'header e gli slider sono ok; non sono riuscito a riprodurre le "3 card con label tagliate" (il preset è un dropdown, non 3 card affiancate). Serve il breakpoint esatto in cui le vedi tagliate. |
| R18 | **Toast "adatta cucina"**: brutto esteticamente e sparisce troppo in fretta | `recipe.tsx:382,1045` | ✅ **FATTO** — durata 5s→**8s** (testo a 2 righe + azione); accento `--tertiary`→`--cta` (verde "successo", coerente con "Calibrazione completata"). Verificato. |
| R19 | **Regola "H2O"** tra gli ingredienti: l'utente dice "non dovrebbe starci" | `recipe-output.tsx` | ⚠️ tensione con INT‑C8 ⬜ (che invece vuole *aggiungere* la sezione H2O) — da riconciliare |
| R20 | **Avviso "sezione in costruzione"** su Impara | `learn.tsx` | 🔬 **NON serve l'avviso** — Impara ha contenuto reale (hero, percorso padellino, glossario, troubleshooting, pre-ferments). "Inizia il percorso" porta a una ricetta (`/recipe/padellino_torino`), non a un percorso didattico vero → manca il *concetto percorso*, che resta INT‑E2 (evoluzione), non un cartello "in costruzione". |

---

## 🧠 Ripensamenti di prodotto (design prima del codice)

| # | Osservazione | Dove | Stato |
|---|---|---|---|
| R21 | **Navigation bar laterale** (viewport largo): valutare se è il pattern giusto / cosa fanno gli altri | `app-shell.tsx` | 🔁 INT‑NAV (in corso) |
| R22 | **Scelta del momento** ("stasera/domani pranzo…") poco utile → renderla intelligente (suggerita dal motore) o toglierla | `user-needs.tsx` | ⚠️ INT‑C2 ✅ / C6 — ancora percepita poco utile; valutare rimozione |
| R23 | **Suggerimenti "recenti"** su farine/lieviti nella dispensa: forse inutili → togliere? | `recipe-setup-panel.tsx` | 🆕 |
| R24 | **Canonica ⇄ personalizzata**: "Torna all'originale" è monodirezionale e senza feedback → deve diventare **toggle con stato chiaro** | `recipe-view.tsx` | 🆕 |
| R25 | **"Personalizza parametri"**: preset iniziale ("bilanciato") non evidenziato / default non attivo; "Smart link" da spiegare; **X ambigua** (annulla o conferma?) | `recipe-configurator.tsx` | ⚠️ INT‑C5 ✅ (smart link narrativo) — restano preset non evidenziato + X ambigua |
| R26 | **Timeline/procedimento**: duplicazione della scelta giorno; spiegare differenza "ottimizza orari" (abitudini) vs tempi precisi | `recipe-output.tsx` | 🔁 INT‑C6/C7 (estensione) |
| R27 | **Profilo: forno deve essere multi-select**, non esclusivo | `profile.tsx` | 🆕 |
| R28 | **Profilo: opzioni forse inutili** (impastatrice, utensili) → o le usa il motore e lo comunichiamo, o le togliamo | `profile.tsx`, `pizza-engine.ts` | 🆕 |
| R29 | 🔑 **Farciture vs topping**: ciaccino, focaccia genovese ecc. non hanno topping ma **farciture** → modellare il concetto per stile | `topping-library.ts`, `pizza-engine.ts` | 🔶 **PARZIALE** — creata la farcitura `ciaccino` (closed_stuffed). La focaccia genovese è correttamente "bianca" (olio+sale, non farcita). Gli altri stuffed (fugazzeta, calzone) erano già coperti. Resta da decidere se serve un *tipo* "farcitura" distinto a livello di dominio (oggi è un ToppingRecipe `closed_stuffed`). |
| R30 | 🔑 **Topping per-stile, non cross**: margherita romana ≠ verace napoletana → eliminare logica cross-style e "topping adatto all'impasto" (tenere da parte l'idea) | `topping-library.ts` | ✅ **FATTO** — `getRecipesByAuthenticity`: (1) un topping "classico" è "Alternativa" solo se **non** ancorato a stili specifici → Detroit/Chicago/Montanara/baresane non appaiono più sotto Napoletana; (2) **layout-aware**: stili sealed (closed_stuffed, double_thin_sheet) accettano solo topping compatibili → ciaccino/fugazzeta/recco/calzone mostrano solo la loro farcitura. Aggiunto `compatible_layouts` al calzone. Verificati 5 stili. |
| R31 | 🔑 **Persistenza/architettura**: dispensa, farine, ricette salvate → dove vivono? Oggi non sembra pensato. Da disegnare | trasversale | 🆕 (grosso) |
| R32 | **Affordance "Tutto pronto"** in riconfigura profilo sembra cliccabile ma non lo è (solo "Inizia" alla fine) | `profile.tsx` | 🆕 |
| R33 | **Classificazione stili** + filtri aggiuntivi da rivedere | `recommended-styles.tsx` | 🔁 INT‑E1 (peso culturale) area |

---

## Sintesi per i prossimi passi

**Nodi strutturali (🔑)** — vanno disegnati prima di codare:
- R29 + R30 — modello **farciture vs topping**, per-stile (risolve anche il bug R1)
- R31 — **persistenza** di dispensa / farine / ricette salvate

**Da verificare con priorità (⚠️ ri-segnalati malgrado ✅ nel piano)** — possibili regressioni o fix che non comunicano:
- R1 (condimento vuoto), R9 (header piccolo), R13/R15 (copy stili+setup), R17 (label tagliate), R22 (scelta momento), R25 (smart link/preset)

**Bug nuovi azionabili subito:**
- R2, R3, R4, R5, R6

**Decisione aperta da chiarire:** R19 — la "Regola H2O" va tolta (utente) o aggiunta/strutturata (INT‑C8)? Sono in contraddizione.
